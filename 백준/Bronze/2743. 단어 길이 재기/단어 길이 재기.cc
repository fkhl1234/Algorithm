#include <iostream>
#include <cstdio>
#include <vector>
#include <algorithm>
#include <cmath>
#include <stack>
#include <queue>

using namespace std;

//Globals

int main(int argc, char* argv[]) {
	int index = 0;
	char s;
	while((s = getchar()) != '\n') {
		index++;
	}
	
	printf("%d\n", index);
}