#include <iostream>
#include <cstdio>
#include <vector>
#include <algorithm>

using namespace std;

int main() {
     int x, y, w, h;

     scanf("%d %d", &x, &y);
     
     printf("%d\n", x*y);

     return 0;
}